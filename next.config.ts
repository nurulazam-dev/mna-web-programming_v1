import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: [
      "nurulazam-dev.web.app",
      "img.freepik.com",
      "images.unsplash.com",
    ],
  },
};

export default nextConfig;
